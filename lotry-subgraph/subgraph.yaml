specVersion: 1.3.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: TokenLaunchpad
    network: base-sepolia
    source:
      address: "0x38eade322F5dE73DC5bc553528bC51ae15FAee1D"
      abi: TokenLaunchpad
      startBlock: 27207980
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      entities:
        - OwnershipTransferred
        - TokenCreated
      abis:
        - name: TokenLaunchpad
          file: ./abis/TokenLaunchpad.json
      eventHandlers:
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: TokenCreated(indexed address,string,string)
          handler: handleTokenCreated
      file: ./src/token-launchpad.ts
templates:
  - name: BondingCurvePool
    kind: ethereum/contract
    network: base-sepolia
    source:
      abi: BondingCurvePool
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/bonding-curvepool.ts
      entities:
        - TradeEvent
      abis:
        - name: BondingCurvePool
          file: ./abis/BondingCurvePool.json
      eventHandlers:
        - event: TradeEvent(indexed address,uint256)
          handler: handleTradeEvent

